# roadkeep's vendored engine — restored by `roadkeep install --vendor`
.roadkeep/

# dependencies and build output
node_modules/
dist/
out/
release/

# local environment
.env
.env.local
*.log

# os
.DS_Store
Thumbs.db

# machine-local Claude Code settings (absolute paths)
.claude/settings.local.json

# TypeScript project-reference build info
.tsbuild/
*.tsbuildinfo

# per-run Electron profiles (dev and smoke keep their caches apart)
.dev-profile/
.smoke-profile/
