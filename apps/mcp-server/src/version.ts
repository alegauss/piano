/**
 * The plugin's version, which is this server's.
 *
 * Kept by hand beside the plugin manifest and checked against it by a test,
 * because the two are read by different people at different times: Claude Code
 * shows the manifest, and the server reports this. The link to the app carries
 * a version of its own, since the app and the plugin are updated separately.
 */
export const PLUGIN_VERSION = '0.1.0'
